# WebSocket Server URL
# For local development, the default is ws://localhost:3002
# For network/mobile access, set to your machine's IP, e.g.:
# VITE_WS_URL=ws://192.168.1.100:3002
VITE_WS_URL=

# Project path (optional, will be fetched from server if not set)
# VITE_PROJECT_PATH=/path/to/your/project
VITE_PROJECT_PATH=

# Projects directory path for directory browser (optional)
# Pre-fills the project selector with this path for easier project discovery
# Example: VITE_PROJECTS_PATH=/home/user/projects
VITE_PROJECTS_PATH=

# Vite dev server port (optional, defaults to 3000)
VITE_PORT=

# Allowed hosts for Vite dev server (comma-separated)
# Example: .claude.do,code.claude.do,dev.example.com
# If not set, no additional hosts are allowed by default
VITE_ALLOWED_HOSTS=
